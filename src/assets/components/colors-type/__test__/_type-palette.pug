mixin type-palette(options)
    -
        var typeClassName = options.type + " type-palette__type"
        var textClassName = options.type

    .type-palette
        div(class = typeClassName)= options.type
        p(class = textClassName)= options.text
